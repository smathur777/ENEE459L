# ENEE459L
Code repo for course ENEE459L: Embedded AI & IoT Laboratory.
Run tests on Windows in venv: python -m pytest tests/test_public.py -v

Pull Aditya's changes onto main w/ git fetch upstream
git reset --hard upstream/main
push to remote: git push origin main --force-with-lease

put my sol's on top: git checkout solutions
rebase onto main: git rebase main
push to remote: git push --force-with-lease origin solutions


